version: '3'
services:
  mongo:
    image: mongo:4.4
    ports:
      - 27017:27017
    volumes:
      - mongodb:/data/db
      - mongodb_config:/data/configdb
  pisignage-server:
    build:
      context: .
    environment:
      MONGOLAB_URI: mongodb://mongo:27017/pisignage-server-dev
    ports:
      - 3000:3000
    volumes:
      - media:/media
      - data:/data
    depends_on:
      - mongo

volumes:
  mongodb:
  mongodb_config:
  media:
  data:
